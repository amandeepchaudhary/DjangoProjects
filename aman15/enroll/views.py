from django.shortcuts import render

# Create your views here.
# Data base in admin that is automatically installed in django so that is why we dont need to do anything except the commands "python manage.py makemigrations" "python manage.py showmigrations" "python manage.py migrate" and go to the ip address and add "/admin/" the database will open automatically 